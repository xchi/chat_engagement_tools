/**
 * SentimentPanel — FEATURE (T8): live chat-sentiment chart for the creator
 * dashboard, updating as the stream clock advances, from GET /api/sentiment
 * (SentimentResponse in src/types/features.ts). Shows the score trend and
 * the positive/neutral/negative breakdown. Draft stub — replace freely.
 */
export default function SentimentPanel() {
  return null;
}
