/**
 * MomentsGraph — FEATURE (T6): YouTube "most replayed"-style engagement
 * curve rendered on top of PlayerControls' seek bar and synchronized with
 * it. Peaks = past moments with the most unique-chatter interaction, from
 * GET /api/highlights (HighlightsResponse in src/types/features.ts).
 * Hover/scrub reveals the curve + moment labels; clicking a peak seeks the
 * video; the curve extends live as the stream clock advances.
 * Draft stub — replace freely.
 */
export default function MomentsGraph() {
  return null;
}
