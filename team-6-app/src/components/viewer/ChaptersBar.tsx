/**
 * ChaptersBar — FEATURE (T7): chapter markers/labels on or under the video
 * timeline. Chapters "appear" live as the stream clock passes their
 * start_seconds, mocked as topic changes from the video captions, from
 * GET /api/chapters (ChaptersResponse in src/types/features.ts).
 * Draft stub — replace freely.
 */
export default function ChaptersBar() {
  return null;
}
